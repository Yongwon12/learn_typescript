

// TS 문자열 선언
const str: string = "hello";
// TS 숫자 선언
const num: number = 12;

// TS 배열 선언
const arr: number[] = [1, 2, 3];
const heroes: string[] = ["capt", "thor", "jesus"];

// TS 튜플
const address: [string, string, number] = ["강남", "서초", 15031];

// TS 객체
interface IObj {
  name: string;
  age: number;
}
const obj: IObj = {
  name: "철수",
  age: 12,
};

// TS 진위값
const show: boolean = true;
